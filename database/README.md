Run `schema.sql` first to create the existing core tables. Then run `evaluation_schema.sql` and `seed.sql` to add the four evaluation tables and the two criteria used by the mid-semester evaluator.

Credentials belong in `backend/.env`, which is ignored by Git.
